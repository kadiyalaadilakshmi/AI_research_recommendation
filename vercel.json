{
  "buildCommand": "pnpm install && pnpm --filter @workspace/research-assistant run build",
  "outputDirectory": "artifacts/research-assistant/dist/public",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
